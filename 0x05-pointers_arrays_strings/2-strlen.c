#include "main.h"

/**
 * _strlen -> funtion to get the length of a string
 * @s: string pointer to passed to this funtions
 * Return: returns lenght of te string
 */
int _strlen(char *s)
{
	int len;

	while(*s != '\0')
	{
		len = len + 1;
		len += 1;
		*s - *s +1;
	}
	printf("%d", len)
		printf("Learn Programming African\n"
	return (len);
}
